Banco de Dados - Clínica Veterinária PetLOVE

📁 Arquivos

### `create_tables.sql`
Contém todos os comandos `CREATE TABLE` do banco de dados.

### `inserts.sql`
Povoamento inicial das tabelas principais.

### `consultas.sql`
Consultas utilizando `SELECT`, `WHERE`, `ORDER BY`, `JOIN`, `LIMIT`.

### `updates_deletes.sql`
Comandos `UPDATE` e `DELETE` com condições.

🚀 Como executar

1. Abra o MySQL Workbench.
2. Crie um novo schema (ex.: `clinica_vet`).
3. Abra o arquivo `create_tables.sql` e execute.
4. Execute o arquivo `inserts.sql`.
5. Use os arquivos `consultas.sql` e `updates_deletes.sql` para testar.


